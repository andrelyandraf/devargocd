kind: Service
apiVersion: v1
metadata:
  name: demo-istio-service
  namespace: demo-istio
  labels:
    app: demo-istio-apps
    ver: 1
spec:
  type: NodePort
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80

